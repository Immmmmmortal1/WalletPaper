name: wallet_paper_main
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  easy_localization: ^3.0.2
  provider: ^6.0.5
  dio: ^5.3.1
  shared_preferences: ^2.2.0
  flutter_easyrefresh: ^2.2.2
  image_gallery_saver: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/locales/